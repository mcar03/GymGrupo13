extends ../templates/layout

block content
  h1 Eliminar Clase
  p ¿Estás seguro de que deseas eliminar la clase: #{clase.nombre}?
  form(action=`/clases/delete/${clase.clase_id}`, method="POST")
    button(type="submit") Sí, Eliminar
    a(href="/clases") No, Volver a la lista de clases
